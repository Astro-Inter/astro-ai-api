apiVersion: v1
kind: Service
metadata:
  name: astro-ai-api
  labels:
    app.kubernetes.io/name: astro-ai-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: astro-ai-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
