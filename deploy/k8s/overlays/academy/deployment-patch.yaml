apiVersion: apps/v1
kind: Deployment
metadata:
  name: astro-ai-api
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-read
